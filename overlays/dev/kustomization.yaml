bases:
- ../../base

commonLabels:
  app: dev


namespace: dev 

commonAnnotations:
  app: kustom-dev

namePrefix:
  dev-

nameSuffix:
  -v1

patches:
- wordpress-service-port.yaml 

# configMapGenerator:
# - name: mykustom-map
#   env: config.properties